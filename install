acceptance if true then show
if alergy then false and skip
if permission granded only then proceed
filter permission.name(rating) let permission list
get permission then show
if concent verbally and by voice true then
locate package manager install
if installed ! check version 
if Auto-updates on package manager upgrade
if trial version include scent only
if compatibility return true
Full featured version:
Perceivable Operatable Understandable Robust
if Depentency on Standalone then skip install
Browser display

